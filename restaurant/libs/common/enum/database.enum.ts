export enum DbConfig {
  Redis = 'db.redis',
  Postgres = 'db.postgres',
  // Mongo = 'db.mongo',
}

export enum DbName {
  Redis = 'redis',
  Postgres = 'postgres',
  // Mongo = 'mongo',
}

export enum Sort {
  Asc = 'ASC',
  Desc = 'DESC',
}
