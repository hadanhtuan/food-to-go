export enum EDbConfig {
  Redis = 'db.redis',
  Postgres = 'db.postgres',
  // Mongo = 'db.mongo',
}

export enum EDbName {
  Redis = 'redis',
  Postgres = 'postgres',
  // Mongo = 'mongo',
}

export enum ESort {
  Asc = 'ASC',
  Desc = 'DESC',
}
