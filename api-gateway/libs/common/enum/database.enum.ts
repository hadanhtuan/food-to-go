export enum DbConfig {
  Redis = 'db.redis',
  Postgre = 'db.postgre',
  // Mongo = 'db.mongo',
}

export enum DbName {
  Redis = 'redis',
  Postgre = 'postgre',
  // Mongo = 'mongo',
}

export enum Sort {
  Asc = 'ASC',
  Desc = 'DESC',
}
